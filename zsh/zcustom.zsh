# Custom

## Alias'
### Update
alias update="bu && sudo softwareupdate -ia && nu && gu && pipu && hash -r"
### Midnight Commander with xoria
alias mc="mc -x -S xoria256.ini"
### Code dir
hash -d code=$HOME/code

